{
  "name": "fontana-web",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "test": "npm run build && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  }
}
